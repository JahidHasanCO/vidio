/// A model representing an audio resource.
///
/// The [AudioModel] class holds the URL of an audio file.
class AudioModel {

  /// Creates an [AudioModel] with an optional [url].
  AudioModel({this.url});
  /// The URL of the audio resource.
  final String? url;
}
